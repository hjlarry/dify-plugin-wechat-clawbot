[project]
name = "weixin_http_bridge"
version = "0.1.0"
description = "Dify extension plugin that wraps Weixin bot HTTP APIs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin==0.7.1",
    "httpx>=0.28.1,<0.29",
    "segno==1.6.6",
]

[dependency-groups]
dev = []
